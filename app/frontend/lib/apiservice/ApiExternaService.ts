/**
 * ApiExternaService - Frontend
 * Este servicio se implementa en el backend para llamadas API-API.
 * En frontend, las llamadas se hacen al propio backend que integra las APIs externas.
 */

// Nota: Los métodos GetUsuarios e InsertUsuario se implementan en el backend.
// El frontend llama a los endpoints del backend que usan estos servicios.

export class ApiExternaService {
  // Este archivo existe por cumplimiento de la solicitud, pero la lógica está en backend.
}